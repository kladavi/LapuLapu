# LapuLapu — Ignore Rules

# Dependencies
node_modules/
.next/

# V4.0 Sprint 25 - Quartz pilot install (vendored third-party repo, not committed).
# The two pilot patches (Windows junction fallback + og-image disable) are documented
# in quartz-pilot-review.md. If Sprint 25 review recommends DEPLOY, a follow-up sprint
# will decide vendor vs. submodule strategy for tracking Quartz in git.
quartz-site/

# V4.0 Sprint 26 - Quartz portal is emitted into ui/public/quartz/ for local hosting
# via the Next.js dashboard (localhost:3000/quartz/). The emitted HTML is a build
# artifact of the canonical model; it is regenerated by scripts/deploy-quartz-local.ps1
# and never committed.
ui/public/quartz/

# Local Next.js build artifacts (build logs, dev server logs)
/next-build.out
/next-build.err
/next-dev.out
/next-dev.err
/next-start.out
/next-start.err

# Pipeline transcripts (local audit only)
logs/

# OS artefacts
.DS_Store
Thumbs.db

# Temp / backup files
*.tmp
*.bak
~$*

# NOTE: 00-context/generated/current-focus.md and current-focus.json
# are intentionally NOT ignored — they must be Git-tracked for traceability.
